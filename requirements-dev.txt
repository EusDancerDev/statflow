# Development requirements for statflow
# ---------------------------------------------------------------------------
# Typical workflow (from repo root, after a venv):
#   1. pip install -r requirements-dev.txt
#   2. pip install -e .[dev]
# Step 1: Git checkouts of interdependent packages (optional) plus the scientific
# stack, xarray, and dev/docs tooling. Step 2: editable statflow with [dev], which
# pulls filewise and pygenutils from PyPI if not already satisfied — prefer a clean
# venv or use only PyPI (skip the Git lines) to avoid duplicate installs.
# Mirrors pyproject.toml: [project.dependencies], optional [climate] / [dev] / [docs].
# ---------------------------------------------------------------------------

# Interdependent packages — Git (optional; latest main from source)
filewise @ git+https://github.com/EusDancerDev/filewise.git
climarraykit @ git+https://github.com/EusDancerDev/climarraykit.git
pygenutils @ git+https://github.com/EusDancerDev/pygenutils.git

# Core scientific stack (aligns with [project.dependencies])
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Climate stack — xarray matches [project.optional-dependencies] climate / dev
# (climarraykit is provided by the Git line above, or by pip install -e .[dev])
xarray>=2024.2.0

# [project.optional-dependencies] dev
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
isort>=5.0.0
mypy>=1.0.0
flake8>=6.0.0

# [project.optional-dependencies] docs
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0
nbsphinx>=0.8.0
